/** Bundle marker for the Oh-DSH-Desktop plugin collection. */

/** Stable Cordis plugin name when the bundle package is mounted directly. */
export const name = 'oh-dsh-desktop-bundle'

/** The bundle's patch mounts the concrete plugins; this marker is inert. */
export function apply(): void {}
